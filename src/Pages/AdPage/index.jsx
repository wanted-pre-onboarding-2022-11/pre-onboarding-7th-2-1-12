import React from "react";
// import { useGetData } from "../../hooks/useGetData";
// import { ListImp } from "../../utils/adList";

const AdPage = () => {
  // const list = useGetData();
  // const sortData = new ListImp(list?.payload?.ads);
  // console.log(sortData.sorted("status", null));
  return <div>AdPage</div>;
};

export default AdPage;
